// PAGE 1 — the landing page.
//
// The site is now styled from your Figma design, so this page is built in real
// code rather than pasted in as an image: the type stays sharp at every size,
// it reflows on a phone, and it can animate.
//
// If you'd still rather drop the Figma export straight in as one image, set
// `artwork` to its path (e.g. '/design/landing.png') and it takes over.

export const landing = {
  artwork: null,
  fit: 'cover', // 'cover' fills the screen (may crop) | 'contain' shows all of it

  greeting: 'happy 19th birthday to',
  handle: 'user020807', // the orange italic bit
  name: 'Chrissy Wissy', // rendered in caps
}
